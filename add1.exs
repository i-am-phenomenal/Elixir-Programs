defmodule Aditya do
   import IO
  # import String
    def abc do
    #  a = gets(" Enter your choice 1) Sum 2) product")
    a = gets("Enter number 1")
    {num1, _} = String.to_integer(to_char_list(a))

    end
   end
