defmodule Aditya do
    def abc do
      #  list = [1,2,3,4,5]
        #func2(list, 0, list)
        list = [1 | [2,3]]
    # end
    # def func2([head | tail], counter, list) do
    #  if counter < length(list) do
    #     list = [Enum.at(tail, counter) | tail]
    #
    #     IO.inspect list
    #     #IO.inspect " Tail of the list is #{tail}"
    #     IO.inspect tail
    #     func2(list, counter + 1, list)
    #     else
    #       list
    #  end

    end
end
