defmodule Aditya do
  import IO
   def abc do
      a = gets(" Enter a string")
      str = String.trim(a)
      len = String.length(str)
      list = String.graphemes(str)
      func2(list, len, )
    end
     def func2(list, len) do

      end
  end
